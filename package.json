{
  "name": "freef1-server",
  "version": "2.0.0",
  "description": "FreeF1 — live site, visitor tracking, admin dashboard, stream override",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check admin/admin.js && node scripts/smoke-test.js",
    "smoke": "node scripts/smoke-test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.2",
    "express-session": "^1.17.3"
  }
}
